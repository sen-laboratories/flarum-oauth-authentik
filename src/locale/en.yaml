fof-oauth:
  lib:
    providers:
      authentik: Authentik
  admin:
    settings:
      providers:
        authentik:
          description: "Authentik OAuth provider for Flarum."
          client_id_label: Client ID
          client_secret_label: Client Secret
          base_url_label: External Base URL (public server address)
          internal_url_label: Internal Base URL (optional, e.g. http://192.168.0.x:9000)
          
